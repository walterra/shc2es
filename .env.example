# Bosch Smart Home Controller (required for yarn poll)
BSH_HOST=192.168.x.x
BSH_PASSWORD=your_system_password

# Logging (optional)
# LOG_LEVEL=info

# Elasticsearch (required for yarn ingest commands)
ES_NODE=https://localhost:9200
ES_USER=elastic
ES_PASSWORD=your_es_password

# Index prefix (optional, default: smart-home-events)
# ES_INDEX_PREFIX=smart-home-events

# Kibana (optional, for dashboard import during yarn ingest:setup)
# KIBANA_NODE=https://localhost:5601

# OpenTelemetry SDK (recommended for APM instrumentation)
# OTEL_SERVICE_NAME is set per-script in package.json (e.g., shc2es-poll)
# These resource attributes are shared across all scripts
OTEL_RESOURCE_ATTRIBUTES=service.version=1.0.0,deployment.environment=development

# Direct OTLP endpoint (optional, only if NOT using local collector)
# OTEL_EXPORTER_OTLP_ENDPOINT=https://your-deployment.apm.region.cloud.es.io
# OTEL_EXPORTER_OTLP_HEADERS=Authorization=Bearer YOUR_APM_SECRET_TOKEN

# EDOT Collector (required for yarn otel:collector commands)
# Generate API key: Kibana -> Stack Management -> API Keys -> Create API key
ELASTIC_API_KEY=your_api_key_here

# Collector ES endpoint (required for collector, separate from ES_NODE)
# The collector runs in Docker, so localhost won't work. Use one of:
#   - https://host.docker.internal:9200  (ES running on host machine)
#   - https://es-local-dev:9200          (ES from yarn es-dev:start, add network to docker-compose)
#   - https://your-cloud-es.com:443      (cloud/remote ES)
COLLECTOR_ES_NODE=https://host.docker.internal:9200
